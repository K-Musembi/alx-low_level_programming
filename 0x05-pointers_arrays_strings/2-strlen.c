#include "main.h"

/**
 * _strlen - length of string
 * @s: pointer input
 *
 * Return: length
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		++len;
		s++;
	}
	return (len);
	_putchar('\n');
}
